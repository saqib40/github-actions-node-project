name: nodejs CI

on:
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Useless step but phir bii
        run: echo "Installing Node.js dependencies (there aren\'t any)..."

      - name: "Running application tests"
        run: node index.test.js